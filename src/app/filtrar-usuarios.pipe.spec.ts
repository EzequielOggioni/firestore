import { FiltrarUsuariosPipe } from './filtrar-usuarios.pipe';

describe('FiltrarUsuariosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarUsuariosPipe();
    expect(pipe).toBeTruthy();
  });
});
