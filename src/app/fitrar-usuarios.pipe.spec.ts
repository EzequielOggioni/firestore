import { FitrarUsuariosPipe } from './fitrar-usuarios.pipe';

describe('FitrarUsuariosPipe', () => {
  it('create an instance', () => {
    const pipe = new FitrarUsuariosPipe();
    expect(pipe).toBeTruthy();
  });
});
